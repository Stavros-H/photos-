# Running the project

This project is a standalone static photo gallery served from `index.html`.

## Preview

The Replit workflow runs:

```bash
python3 -m http.server 5000 --bind 0.0.0.0
```

Use the Replit Preview pane to view the gallery.